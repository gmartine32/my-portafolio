---
import { Button } from "../ui/button";
---

<script>
  import { animateHero } from "../../scripts/hero-animation";

  animateHero();
</script>

<section
  id="home"
  class="min-h-screen flex items-center justify-center relative overflow-hidden"
>
  <!-- Fondos -->
  

  <!-- Fondos tipo lámpara de lava, formas pequeñas y colores vibrantes -->
  <div class="absolute inset-0 pointer-events-none z-0">
    <div class="absolute top-[10%] left-[15%] w-24 h-24 bg-pink-400/40 blur-2xl hero-float-bg lava-1"></div>
    <div class="absolute top-[30%] left-[60%] w-20 h-20 bg-blue-400/40 blur-2xl hero-float-bg lava-2"></div>
    <div class="absolute top-[60%] left-[25%] w-16 h-16 bg-yellow-400/40 blur-2xl hero-float-bg lava-3"></div>
    <div class="absolute top-[70%] left-[70%] w-28 h-28 bg-purple-400/40 blur-2xl hero-float-bg lava-4"></div>
    <div class="absolute top-[50%] left-[45%] w-14 h-14 bg-green-400/40 blur-2xl hero-float-bg lava-5"></div>
    <div class="absolute top-[80%] left-[10%] w-18 h-18 bg-orange-400/40 blur-2xl hero-float-bg lava-6"></div>
  </div>

  <!-- Contenido -->
  <div class="container mx-auto px-6 text-center relative z-10">
    <div class="max-w-4xl mx-auto">
      <p class="hero-greeting invisible text-neon-blue text-lg mb-4 font-light tracking-wider">
  Hola, soy
</p>

<h1 class="hero-name invisible text-6xl md:text-8xl font-bold mb-6">
  Gian Martínez
</h1>

<h2 class="hero-title invisible text-2xl md:text-3xl text-accent mb-8 font-light">
  Frontend Developer | Ingeniero en Sistemas
</h2>

<p class="hero-description invisible text-lg text-muted-foreground max-w-2xl mx-auto mb-12 leading-relaxed">
  Ingeniero en Sistemas y Desarrollador Full Stack con especialización en Frontend (React, Next.js, Node.js y React Native). Experto en despliegue de aplicaciones mediante Docker y Nginx, con experiencia en infraestructura en la nube. He trabajado en sectores energético, gubernamental y comercial, aplicando principios SOLID, arquitectura limpia y arquitectura hexagonal para garantizar soluciones escalables y mantenibles. Reconocido por liderazgo técnico, adaptabilidad y comunicación efectiva, orientado a entregar resultados de alta calidad.
</p>

      <div class="hero-cta invisible flex flex-col sm:flex-row gap-4 justify-center items-center mb-16">
        <a
          href="#projects"
          class="bg-gradient-primary hover:shadow-glow transition-all duration-300 text-primary-foreground font-medium px-8 py-3 rounded-md shadow focus:outline-none focus:ring-2 focus:ring-primary focus:ring-offset-2 inline-flex items-center justify-center"
          onclick="document.getElementById('projects')?.scrollIntoView({ behavior: 'smooth' }); return false;"
        >
          Ver Proyectos
        </a>
        <a
          href="/documents/cv-gian-martinez.pdf"
          download="Gian-Martinez-CV.pdf"
          class="border-primary/20 text-primary bg-background hover:bg-primary/10 hover:border-primary/40 px-8 py-3 rounded-md font-medium transition-all duration-300 shadow focus:outline-none focus:ring-2 focus:ring-primary focus:ring-offset-2 inline-flex items-center justify-center gap-2"
        >
          <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4" /></svg>
          Descargar CV
        </a>
      </div>
    </div>
  </div>
</section>

<style>
  /* Ajuste en global.css para movimiento más orgánico y tamaño controlado */
@keyframes lavaMove {
  0% {
    transform: translateY(0) scale(1) skew(0deg, 0deg) rotate(0deg);
    border-radius: 50% 50% 50% 50%;
    opacity: 0.6;
  }
  15% {
    transform: translateY(-12px) scale(1.05) skew(4deg, 2deg) rotate(-2deg);
    border-radius: 55% 45% 52% 48%;
    opacity: 0.7;
  }
  35% {
    transform: translateY(8px) scale(0.97) skew(-3deg, -5deg) rotate(3deg);
    border-radius: 48% 52% 46% 54%;
    opacity: 0.8;
  }
  60% {
    transform: translateY(-10px) scale(1.03) skew(2deg, 6deg) rotate(-1deg);
    border-radius: 53% 47% 58% 42%;
    opacity: 0.7;
  }
  100% {
    transform: translateY(0) scale(1) skew(0deg, 0deg) rotate(0deg);
    border-radius: 50% 50% 50% 50%;
    opacity: 0.6;
  }
}
.hero-float-bg {
  animation: lavaMove 9s cubic-bezier(0.77,0,0.175,1) infinite alternate;
  will-change: transform, opacity, border-radius;
  border-radius: 50%;
  mix-blend-mode: lighten;
}
.lava-1 { animation-delay: 0s; animation-duration: 10s; }
.lava-2 { animation-delay: 1.5s; animation-duration: 12s; }
.lava-3 { animation-delay: 2.5s; animation-duration: 14s; }
.lava-4 { animation-delay: 3.5s; animation-duration: 11s; }
.lava-5 { animation-delay: 4.5s; animation-duration: 13s; }
.lava-6 { animation-delay: 5.5s; animation-duration: 15s; }
</style>